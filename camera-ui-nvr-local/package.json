{
  "displayName": "NVR (Local)",
  "name": "@calebcall/camera-ui-nvr-local",
  "version": "0.1.0",
  "description": "An open-source, local NVR hub for camera.ui: stores and manages video recordings from your cameras entirely on your own hardware, with no cloud dependency.",
  "author": "calebcall (https://github.com/calebcall/plugins)",
  "main": "./main.go",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && go build -o /dev/null ./src/",
    "bundle": "npm run build && cui bundle",
    "bundle:dev": "npm run build && cross-env MODE=development cui bundle",
    "format": "gofmt -w ./src/* && go fix ./src/...",
    "install-updates": "npm i --save --force && go mod tidy",
    "lint": "staticcheck ./src/... && golangci-lint run ./src/...",
    "prepublishOnly": "node -e \"if(!process.env.SAFE_PUBLISH){console.error('Error: Please use @camera.ui/cli to publish the plugin:\\n  npm run publish:alpha\\n  npm run publish:beta\\n  npm run publish:latest\\n');process.exit(1)}\"",
    "publish:alpha": "npm i --save --force && npm run bundle && cui publish --alpha",
    "publish:beta": "npm i --save --force && npm run bundle && cui publish --beta",
    "publish:latest": "npm i --save --force && npm run bundle && cui publish --latest",
    "test": "go test ./...",
    "update": "updates --update ./"
  },
  "devDependencies": {
    "@camera.ui/cli": "~0.0.65",
    "@camera.ui/sdk": "~0.0.22",
    "@types/node": "^26.1.1",
    "cross-env": "^10.1.0",
    "rimraf": "^6.1.3",
    "typescript": "5.9.3",
    "updates": "^17.19.1"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/calebcall/plugins/issues"
  },
  "engines": {
    "camera.ui": ">=2.0.11",
    "node": ">=22.0.0"
  },
  "homepage": "https://github.com/calebcall/plugins/tree/main/camera-ui-nvr-local#readme",
  "keywords": [
    "camera-ui-plugin",
    "nvr",
    "recording",
    "local"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/calebcall/plugins.git",
    "directory": "camera-ui-nvr-local"
  },
  "os": [],
  "cpu": [],
  "optionalDependencies": {
    "@calebcall/camera-ui-nvr-local-darwin-arm64": "0.1.0",
    "@calebcall/camera-ui-nvr-local-darwin-amd64": "0.1.0",
    "@calebcall/camera-ui-nvr-local-linux-amd64": "0.1.0",
    "@calebcall/camera-ui-nvr-local-linux-arm64": "0.1.0",
    "@calebcall/camera-ui-nvr-local-windows-amd64": "0.1.0",
    "@calebcall/camera-ui-nvr-local-windows-arm64": "0.1.0",
    "@calebcall/camera-ui-nvr-local-linux-amd64-musl": "0.1.0",
    "@calebcall/camera-ui-nvr-local-linux-arm64-musl": "0.1.0"
  }
}
